#'패키지'에는 다양한 함수들이 있음
install.packages("ggplot2")  #ggplot2 패키지 설치
library(ggplot2)             #ggplot2 패키지 로드

#ggplot 패키지의 사용
x<-c("a","a","b","c")
gplot(x) #ggplot 패키지에 있는 gplot()함수 이용

?gplot   #Help 함수, 기능이 궁금한 함수 앞에 ?를 붙여 함수 소개, 파라미터 사용법, 예제 코드 확인 가능